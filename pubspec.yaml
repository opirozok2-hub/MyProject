name: my_project
description: "A new Flutter project."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'  # Это позволит новые версии пакетов без конфликтов

dependencies:
  flutter:
    sdk: flutter
  shared_preferences: ^2.3.2
  crypto: ^3.0.5
  cupertino_icons: ^1.0.8
  http: ^1.2.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0  # Обнови на свежую версию, если нужно

flutter:
  uses-material-design: true